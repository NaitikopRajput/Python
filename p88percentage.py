per =int(input("Enter percentage =>"))
if per > 90 and per<=100:
 print("Grade is A")
elif per > 80 and per <=90:
 print("Grade is B")
elif per >=60 and per <= 80:
 print("Grade is C")
elif per < 60:
 print("Grade is D")